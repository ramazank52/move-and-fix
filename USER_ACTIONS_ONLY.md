# User Actions Only

1. Provide an owner-approved **isolated private-staging TiDB** connection through the secure project setting. The host/database must be clearly non-production; do not send a credential in chat.
2. After reviewing the preflight ledger/schema report, explicitly authorize or reject the ordered 0090–0097 migration run. No `db:push` should be used.
3. Supply lawful, independent `LEGAL_SOURCE_APPROVAL` and `PRODUCT_RELEASE_APPROVAL` records before any country/capability activation; this source work is not such an approval.
4. If external integrations are desired, configure approved sandbox credentials through secure settings and authorize limited synthetic delivery tests. Do not provide secrets in chat.
5. Perform physical iOS and Android tests for camera permissions/manual fallback/AR accuracy and authenticated role workflows; simulator or fixture evidence is not sufficient.
