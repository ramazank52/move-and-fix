# Explore Component Fixture

Run `pnpm explore-fixture` only in development. This app-root exists outside `app/`, imports the production `ExploreScreenView`, and passes immutable in-memory props plus no-op callbacks. It is a component fixture, not authenticated route E2E, a database run, or production evidence.
